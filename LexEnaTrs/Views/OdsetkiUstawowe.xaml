<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:riaControls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.DomainServices" xmlns:my="clr-namespace:LexEnaTrs.Web" xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" x:Class="LexEnaTrs.Views.OdsetkiUstawowe"
    mc:Ignorable="d"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:local="clr-namespace:LexEnaTrs"
    d:DesignHeight="592" d:DesignWidth="800" >
    
    <Grid x:Name="LayoutRoot" Background="White">

        <telerik:RadDomainDataSource AutoLoad="True" Height="0" x:Name="OdsetkiUstawowedds" QueryName="GetOdsTabinOrderQuery" Width="0">
                <telerik:RadDomainDataSource.DomainContext>
                    <my:LexEnaMeritumDomainContext />
                </telerik:RadDomainDataSource.DomainContext>
                    </telerik:RadDomainDataSource>
        <Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="26"/>
				<RowDefinition/>
				<RowDefinition Height="29"/>
			</Grid.RowDefinitions>
           

            <telerik:RadBusyIndicator x:Name="BusyIndicator" IsBusy="{Binding IsBusy, ElementName=OdsetkiUstawowedds}" Grid.Row="1" BusyContent="Przetwarzanie w toku...">
                <telerik:RadGridView  IsSynchronizedWithCurrentItem="false"  Grid.Row="1" x:Name="SprawyGridView" AutoGenerateColumns="false"   IsReadOnly="True" ItemsSource="{Binding DataView, ElementName=OdsetkiUstawowedds}" AutoExpandGroups="True" CanUserFreezeColumns="False" RowIndicatorVisibility="Visible" Margin="0,-25,0,0" >
               
                <telerik:RadGridView.Columns>


                        <telerik:GridViewDataColumn Header="Data Od" DataMemberBinding="{Binding DataP, StringFormat=\{0:d\}}" IsReadOnly="True"  HeaderTextAlignment="Center" MinWidth="5" TextAlignment="Center" />
                        <telerik:GridViewDataColumn Header="Data Do" DataMemberBinding="{Binding DataK, StringFormat=\{0:d\}}" IsReadOnly="True" IsFilterable="false" HeaderTextAlignment="Center" MinWidth="5" TextAlignment="Center" />
                        <telerik:GridViewMaskedInputColumn  Header="Stopa ( rocznie ) w %"  DataMemberBinding="{Binding Proc0}" MinWidth="10" MaskType="Numeric" Mask="n2"   IsFilterable="false"  HeaderTextAlignment="Center" TextAlignment="Center"  />
                        
                            
                    </telerik:RadGridView.Columns>               
                </telerik:RadGridView>
            </telerik:RadBusyIndicator>

           
           
        </Grid >
    </Grid>
</UserControl>
