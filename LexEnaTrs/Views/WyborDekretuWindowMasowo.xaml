<controls:ChildWindow xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="LexEnaTrs.Views.WyborDekretuWindowMasowo"
           Width="400" Height="300" 
           Title="Zadekretuj sprawy do:">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="CancelButton" Content="Anuluj" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="2" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="2" />
        <sdk:Label Grid.Row="0" Content="Prefiks sygnatury"  /><TextBox x:Name="ptbPrefix" Width="120"  Grid.Row="0"/>
        <telerik:RadGridView x:Name="GridViewSelJednostka" RowIndicatorVisibility="Collapsed" Grid.Row="1" IsReadOnly="True"  AutoGenerateColumns="False" ItemsSource="{Binding dictionaryJednostkiWindykacji, Source={StaticResource JednostkaWindykacjiList}}" d:LayoutOverrides="Height" ShowColumnHeaders="False" ShowGroupPanel="False" >
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Nazwa jednostki" DataMemberBinding="{Binding Nazwa}" Width="*" />
                <!--<telerik:GridViewDataColumn DataMemberBinding="(Binding Id}" />-->
                </telerik:RadGridView.Columns>
        </telerik:RadGridView>
                             
            
    </Grid>
</controls:ChildWindow>