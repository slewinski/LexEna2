<controls:ChildWindow
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="LexEnaTrs.Views.WyborDekretuReferentWindow"
           Width="400" Height="300" 
           Title="Zadekretuj sprawy do:">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="CancelButton" Content="Anuluj" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
        <telerik:RadGridView x:Name="GridViewSelJednostka" RowIndicatorVisibility="Collapsed" IsReadOnly="True"  AutoGenerateColumns="False" ItemsSource="{Binding dictionaryUzytkownicy, Source={StaticResource Uzytkownicy}}" d:LayoutOverrides="Height" ShowColumnHeaders="False" ShowGroupPanel="False" >
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="Imię" DataMemberBinding="{Binding Imie}" />
                <telerik:GridViewDataColumn Header="Nazwisko" DataMemberBinding="{Binding Nazwisko}" Width="*" />
                <!--<telerik:GridViewDataColumn DataMemberBinding="(Binding Id}" />-->
                </telerik:RadGridView.Columns>
        </telerik:RadGridView>
                             
            
    </Grid>
</controls:ChildWindow>